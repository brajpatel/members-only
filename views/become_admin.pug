extends layout

block content
  div(class="d-flex flex-column align-items-center mx-auto px-4 py-3 become-admin-container")
    if user.account_status === "admin"
      h1 You are already an admin!

    else
      h1(class="form-title") #{title}
      p(class="text-center") By becoming an admin, you'll be able to do everything a member can, with the addition of being able to delete messages.
      form(class="d-flex flex-column" action="" method="POST")
        label(for="admin_password") #[strong Password]
        input(id="admin_password" name="admin_password" required="true" type="password" autofocus)
        button(class="my-3 create-message-btn" type="submit") Become an Admin